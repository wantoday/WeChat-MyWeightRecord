/// <reference types="miniprogram-api-typings" />

/**
 * 全局补充声明。
 *
 * 官方 `miniprogram-api-typings` 提供 wx / WechatMiniprogram / DB / Page / App 等全局类型，
 * 通过上面的 reference types 引入（见 tsconfig include 中的 ./typings/**\/*.d.ts）。
 * 官方包已声明 console，本文件不再重复。
 */
