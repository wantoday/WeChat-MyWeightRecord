/* 打卡页：录入卡片做了垂直压缩，保证「标题 + 输入框 + 按钮」在矮屏首屏完整可见 */

.entry {
  text-align: center;
  padding: 28rpx 32rpx 24rpx;
}

.entry-input {
  display: flex;
  align-items: baseline;
  justify-content: center;
  margin: 12rpx 0 8rpx;
}

.weight-field {
  font-size: 104rpx;
  font-weight: 300;
  /* 显式高度 + 行高一致：大字号数字（4/8/9 等上下延伸字符）
     在只有 line-height 时行盒会被 input 默认高度裁切，必须给足高度 */
  height: 140rpx;
  line-height: 140rpx;
  min-width: 240rpx;
  text-align: right;
  color: var(--text);
  padding: 0;
}

.unit {
  font-size: 32rpx;
  color: var(--text-sub);
  margin-left: 12rpx;
}

.delta {
  font-size: 26rpx;
  color: var(--text-sub);
  margin-bottom: 16rpx;
}

.entry .btn-primary {
  margin-top: 16rpx;
}

.bmi-line {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.bmi-value {
  font-size: 56rpx;
  font-weight: 500;
}

.bmi-label {
  color: #fff;
  font-size: 24rpx;
  padding: 4rpx 20rpx;
  border-radius: 999rpx;
}

.tip {
  color: var(--text-sub);
  font-size: 26rpx;
}

/* 目标进度条 */
.bar {
  height: 16rpx;
  background: var(--line);
  border-radius: 999rpx;
  overflow: hidden;
}

.bar-fill {
  height: 100%;
  background: var(--brand);
  border-radius: 999rpx;
  transition: width 0.3s ease;
}

.bar-pct {
  margin-top: 8rpx;
  font-size: 26rpx;
  color: var(--text-sub);
  text-align: right;
}
